title: Legal services
description: Legal services authority yadavroy
template:
  repo: legal-services-template
  name: legal-services
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
